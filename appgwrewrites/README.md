### appgwrewrites

Simple utility that reads (from stdin) the (json) output of `az network application-gateway rewrite-rule list` and generates a table with the target host header and host/path condition.
